<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="min-h-screen flex flex-col bg-zinc-900 text-white">
    <!-- Page content wrapper -->
    <div class="flex-1 p-10">
    

    <h2 class="text-3xl mb-5">Profile</h2>
    <h3 class="text-3xl mb-6"> <span class="font-light ">Hello</span> <%= user.name %>! </h3>

    <h5 class="mb-3">You can create a new post 👀</h5>

    <form method="post" action="/post">
        <textarea placeholder="What's on your mind  ?" class=" outline-none pd-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
        <input class="bg-blue-500 px-2 py-2 rounded-md block mt-2" type="submit" value="Create New Post">
    </form>

    <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post){    %>

                <div class="post w-1/3 p-4 border-[1px] border-zinc-700 rounded-md bg-zinc-800 mb-4">
                    <h4 class="text-blue-500 mb-2">
                        <%= post.user?.username || post.user?.name || post.user?.email %>
                      </h4>
                    <p class="text-sm tracking-tight" > <%= post.content %> </p>
                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="">Like</a>
                        <a class="text-zinc-600" href="">edit</a>
                    </div>
                </div>
           <%   }) %>
        
        </div>
    </div>

    <p class="mt-4 text-sm text-gray-400">
        Want to Logout?
        <a href="/login" class="text-blue-400 hover:underline">Click here</a>
      </p>

      <a href="/about" class="text-sm text-blue-400 hover:underline">About This Project</a>

    </div>

      <footer class="text-center text-gray-500 py-4 border-t border-zinc-800">
        &copy; 2025 All rights reserved
      </footer>
      
      
   
</body>
</html> 
